#include <stdio.h>

int main()
{
    // Inputs ---
    // char a = getchar();
    // char a[100];
    // gets(a);
    char ch;
    scanf("%c", &ch);
    // char ch[100];
    // scanf("%s", &ch);

    // Outputs ---
    printf("you entered: ");
    // putchar(a);
    // puts(a);
    printf("%c", ch);
    // printf("%s", ch);

    return 0;
}
