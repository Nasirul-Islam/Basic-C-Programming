#include <stdio.h>

int main()
{
    // Inputs ---
    // 1. single character
    // char a = getchar();
    // 2. bunch of character with space
    // char a[100];
    // gets(a);
    // 3. single character
    // char ch;
    // scanf("%c", &ch);
    // 4. bunch of character withOut space
    // char ch[100];
    // scanf("%s", &ch);

    // Outputs ---
    printf("you entered: ");
    // 1.
    // putchar(a);
    // 2.
    // puts(a);
    // 3.
    // printf("%c", ch);
    // 4.
    // printf("%s", ch);

    return 0;
}
